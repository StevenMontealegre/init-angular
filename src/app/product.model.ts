// Debe estar acompañada de la palabra export para que sea visible en todo
// el directorio del proyecto
export interface Product {
    id: string;
    image: string;
    title: string;
    price: number;
    description: string;
}
